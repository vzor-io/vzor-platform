📜 Project Rules & Guidelines (Gemini.md)
🏗 Контекст проекта
Стек: Next.js (App Router), Tailwind CSS, Shadcn UI.

Стиль кода: Пиши чистый, модульный код. Используй TypeScript для всех новых файлов.

Структура: Компоненты — в components/, страницы — в app/, стили — в app/globals.css.

🛡 Правила безопасности (Для автопилота)
Запрет на удаление: Никогда не используй команды rm -rf или аналогичные для удаления папок без явного подтверждения, даже если включен режим авто-выполнения.

Работа с Git: Перед внесением крупных изменений в структуру проекта, убедись, что текущие изменения закоммичены.

Учетные данные: Никогда не записывай API-ключи или пароли в код. Используй только .env.local.

Обработка ошибок: Если команда в терминале завершилась с ошибкой, прекрати выполнение, проанализируй лог и предложи исправление, прежде чем пробовать снова.

⚙️ Правила выполнения (Orchestration)
Консистентность: Всегда проверяй, не создаешь ли ты дубликаты данных или функций, которые уже есть в проекте.

Скрипты: При установке новых библиотек всегда используй npm (не yarn/pnpm), если в корне уже есть package-lock.json.

Оркестрация: Если задача масштабная, разбей её на подзадачи и используй Agent Manager для параллельного выполнения несвязанных частей (например, верстка страницы и настройка API).

🛠 Workflows & Skills
Используй /check для быстрой проверки состояния проекта перед началом работы.

При создании новых «умений» сохраняй их в agent/skills/.