"""
Простой клиент для Agent Zero - запускает в отдельном процессе
"""
import subprocess
import json
import tempfile
import os

def call_agent_zero(message: str) -> str:
    """Вызывает Agent Zero в его собственном окружении"""
    
    # Пока используем простой mock пока не настроим полноценную интеграцию
    # В производстве здесь будет вызов Agent Zero через subprocess
    
    response = f"""🤖 Agent Zero (DeepSeek - тестовый режим)

Получено: "{message}"

Статус: Работаю в безопасном режиме с ограничениями
- Модель: deepseek-chat  
- Лимит: 5 запросов/мин
- Отключены: terminal, code execution
- Включены: chat, knowledge

Для полной интеграции нужно запустить Agent Zero UI отдельно.

Ответ: Привет! Я Agent Zero, настроен на DeepSeek. Готов помочь в рамках тестового режима.
"""
    
    return response

if __name__ == "__main__":
    # Тест
    print(call_agent_zero("Привет"))
