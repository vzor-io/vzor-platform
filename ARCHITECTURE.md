# AgentVzor.0 — Архитектура системы
> Дата: 2026-02-08 | Сервер: 95.174.95.209 | Домен: vzor-ai.com

---

## Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                    БРАУЗЕР (vzor-ai.com)                     │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              VZOR Chat (index.html)                   │   │
│  │                                                      │   │
│  │  ┌──────────────┐  ┌───────────────────────────┐     │   │
│  │  │  V0          │  │  Brain selector (dropdown) │     │   │
│  │  │ AgentVzor.0  │  │  ├ DeepSeek (default)      │     │   │
│  │  │ brain: DS    │  │  ├ Claude Sonnet           │     │   │
│  │  └──────────────┘  │  ├ Gemini Flash            │     │   │
│  │                     │  └ OpenRouter models       │     │   │
│  │  ┌──────────────────────────────────────────┐    │     │   │
│  │  │  💬 / 🎤  Ввод сообщения                │    │     │   │
│  │  └──────────────────────────────────────────┘    │     │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────┘
                             │ POST /api/task
                             │ { message, model, context }
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                 NGINX (vzor-nginx контейнер)                 │
│           /home/vzor/vzor/config/nginx/default.conf          │
│                                                             │
│  /              → static files (index.html)                 │
│  /api/*         → 172.17.0.1:8000  (VZOR API)              │
│  /agent-zero/*  → 172.17.0.1:5000  (Agent Zero UI)         │
│  /ws/*          → 172.17.0.1:8000  (WebSocket)             │
│  /n8n/, /grafana/, /fm/, /notes/  → другие сервисы         │
└────────────┬──────────────────────────────┬─────────────────┘
             │                              │
             ▼                              ▼
┌────────────────────────┐    ┌──────────────────────────────┐
│     VZOR API :8000     │    │   Agent Zero UI :5000        │
│  /home/vzor/vzor-api/  │    │  https://vzor-ai.com/        │
│                        │    │          agent-zero/          │
│  main.py (FastAPI)     │    └──────────────────────────────┘
│    │                   │
│    ▼                   │
│  multi_model.py        │    ВСЕ сообщения идут
│    │                   │    через AgentVzor.0.
│    │ model="agent-zero"│    Селектор меняет только
│    │ model="deepseek"  │──┐ LLM-бэкенд агента.
│    │ model="claude"    │  │
│    │ model="gemini"    │  │
│    ▼                   │  │
│  agent_zero_client.py  │  │
│    │                   │  │
│    ├─ /switch_model ───┼──┼──► Меняет LLM-мозг
│    │                   │  │
│    └─ /api_message ────┼──┼──► Отправляет сообщение
│                        │  │
│  API KEY: Jq0wL-H8f... │  │
└────────────────────────┘  │
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  AGENT ZERO :5000                            │
│              /home/vzor/agent-zero/                          │
│              systemd: agent-zero.service                     │
│              venv: Python 3.11                               │
│                                                             │
│  run_ui.py (Flask)                                          │
│    │                                                        │
│    ├── /api_message    ← принимает сообщения (API key)      │
│    ├── /switch_model   ← переключает LLM (API key)         │
│    ├── /settings_set   ← UI settings (auth+csrf)            │
│    └── /               ← Web UI                             │
│                                                             │
│  ┌────────────────────────────────────────────────────┐     │
│  │              AgentVzor.0 Framework                  │     │
│  │                                                    │     │
│  │  Промпты: prompts/                                 │     │
│  │    agent.system.main.role.md  → "Я AgentVzor.0"    │     │
│  │    agent.system.main.md       → System Manual       │     │
│  │    fw.initial_message.md      → Приветствие         │     │
│  │                                                    │     │
│  │  Настройки: tmp/settings.json                      │     │
│  │    chat_model_provider: deepseek                   │     │
│  │    chat_model_name: deepseek-chat                  │     │
│  │    api_keys: { deepseek: "sk-..." }                │     │
│  │                                                    │     │
│  │  Инструменты:                                      │     │
│  │    ├ Terminal (bash, ssh)                           │     │
│  │    ├ Code execution (Python, Node.js)              │     │
│  │    ├ Browser (через суб-агента)                    │     │
│  │    ├ Memory (долговременная память)                 │     │
│  │    ├ Scheduler (планировщик задач)                  │     │
│  │    └ Sub-agents (делегирование)                    │     │
│  └────────────────────────────────────────────────────┘     │
│                         │                                    │
│                         ▼ LLM API call                       │
│              ┌─────────────────────┐                         │
│              │   LLM Backend       │                         │
│              │   (переключаемый)   │                         │
│              │                     │                         │
│              │  DeepSeek ← default │                         │
│              │  Claude   ← когда   │                         │
│              │  Gemini     ключи   │                         │
│              └─────────────────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

---

## Файлы и назначение

### Agent Zero: `/home/vzor/agent-zero/`

| Файл | Назначение |
|------|-----------|
| `.env` | DeepSeek API ключ, SAFE_MODE=false, терминал/код включены |
| `run_ui.py` | Главный процесс (Flask, порт 5000) |
| `tmp/settings.json` | Рантайм настройки (модель, ключи, лимиты) |
| `tmp/chats/` | Контексты диалогов |
| `prompts/agent.system.main.role.md` | Идентичность: "Я AgentVzor.0" |
| `prompts/agent.system.main.md` | Системный мануал агента |
| `prompts/fw.initial_message.md` | Приветствие при новом чате |
| `python/api/switch_model.py` | Эндпоинт смены LLM бэкенда |
| `python/api/api_message.py` | Эндпоинт приёма сообщений |
| `python/api/csrf_token.py` | CSRF токены (исправлен для Python 3.11) |

### VZOR API: `/home/vzor/vzor-api/`

| Файл | Назначение |
|------|-----------|
| `main.py` | FastAPI приложение, `/api/task` → `call_model()` |
| `multi_model.py` | Роутинг — все запросы через AgentVzor.0 |
| `agent_zero_client.py` | HTTP клиент: `/switch_model` + `/api_message` |
| `system_prompt.py` | VZOR промпты по категориям (Development, Finance, Real Estate) |
| `db.py` | PostgreSQL: история чатов, задачи |

### Nginx: `/home/vzor/vzor/config/nginx/`

| Файл | Назначение |
|------|-----------|
| `default.conf` | SSL, проксирование всех сервисов |
| `www/index.html` | VZOR v3.22 — фронтенд с 3D-графом и чатом |

### Systemd сервисы

| Сервис | Команда | Порт |
|--------|---------|------|
| `agent-zero.service` | `/home/vzor/agent-zero/venv/bin/python3 run_ui.py` | 5000 |
| `vzor-api.service` | `uvicorn main:app --host 0.0.0.0 --port 8000` | 8000 |

---

## Поток запроса

```
Пользователь пишет "Привет" (brain: DeepSeek)
    │
    ▼
index.html → POST /api/task { message: "Привет", model: "deepseek" }
    │
    ▼
nginx (443) → proxy_pass → VZOR API :8000
    │
    ▼
main.py → call_model("deepseek", "Привет")
    │
    ▼
multi_model.py → call_agent_zero(llm_backend="deepseek")
    │
    ├──► POST :5000/switch_model { provider: "deepseek" }
    │    (только если текущий мозг другой)
    │
    └──► POST :5000/api_message { message: "Привет" }
              │
              ▼
         AgentVzor.0 обрабатывает через DeepSeek API
              │
              ▼
         Ответ: "Привет! Я AgentVzor.0..."
              │
              ▼
         VZOR API сохраняет в PostgreSQL
              │
              ▼
         JSON ответ → nginx → браузер
```

---

## Переключение LLM (Brain Selector)

```
Пользователь кликает "Claude Sonnet" в dropdown
    │
    ▼
JS: selectedModel = "claude"
    │
    ▼
POST /api/task { model: "claude", message: "..." }
    │
    ▼
agent_zero_client.py:
    1. POST /switch_model { provider: "anthropic", model_name: "claude-sonnet-4-5-20250929" }
    2. Agent Zero перезагружает LLM конфиг в памяти
    3. POST /api_message { message: "..." }
    │
    ▼
AgentVzor.0 теперь думает через Claude
```

---

## Docker-контейнеры на сервере

| Контейнер | Порт | Назначение |
|-----------|------|-----------|
| vzor-nginx | 80, 443 | Reverse proxy + SSL + static files |
| vzor-postgres | 5432 | БД: чаты, задачи |
| vzor-redis | 6379 | Кэш |
| vzor-n8n | 5678 | Автоматизация |
| vzor-grafana | 3000 | Мониторинг |
| vzor-prometheus | 9090 | Метрики |
| vzor-trilium | 8081 | Заметки |
| vzor-certbot | — | SSL сертификаты |

Нативные сервисы (systemd):
- `agent-zero.service` → порт 5000
- `vzor-api.service` → порт 8000

---

## URL карта

| URL | Что |
|-----|-----|
| `https://vzor-ai.com/` | Главная страница + 3D + чат |
| `https://vzor-ai.com/api/task` | API чата |
| `https://vzor-ai.com/agent-zero/` | Web UI Agent Zero |
| `https://vzor-ai.com/n8n/` | n8n автоматизация |
| `https://vzor-ai.com/grafana/` | Grafana мониторинг |
| `https://vzor-ai.com/fm` | File Browser |
| `https://vzor-ai.com/notes/` | Trilium заметки |

---

## Статус

| Компонент | Статус |
|-----------|--------|
| AgentVzor.0 работает | ✅ |
| DeepSeek как мозг | ✅ |
| Переключение мозга через API | ✅ |
| Терминал + код исполнение | ✅ |
| UI агента `/agent-zero/` | ✅ |
| Автозапуск (systemd) | ✅ |
| Claude как мозг | ⏳ нужен ANTHROPIC_API_KEY |
| Gemini как мозг | ⏳ нужен GEMINI_API_KEY |
