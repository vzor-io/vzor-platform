# СРОЧНЫЕ ИСПРАВЛЕНИЯ ДЛЯ GEMINI

> **ВАЖНО:** Ты сломал рабочий функционал. Верни как было!

---

## ОШИБКА 1: Названия на втором слайде (INCUBATOR)

### Было (правильно):
- Названия "Development", "Finance", "Real Estate" были **СТАТИЧНЫМИ**
- Они НЕ двигались с облаком точек
- Они были ФИКСИРОВАНЫ на экране (position: fixed)

### Стало (неправильно):
- Названия двигаются вместе с облаком при вращении
- Это сломано

### Как исправить:
```jsx
{/* НАЗВАНИЯ ДОЛЖНЫ БЫТЬ В ОТДЕЛЬНОМ DIV С position: fixed */}
<div className="fixed inset-0 z-10 pointer-events-none flex items-center justify-center gap-16">
    {/* Названия здесь — они НЕ внутри canvas, они поверх него */}
</div>
```

---

## ОШИБКА 2: Синий фон в режиме WORK

### Было (правильно):
- Фон ЧЁРНЫЙ (`#000` или `#050505`) на ВСЕХ экранах
- Одинаковый фон везде

### Стало (неправильно):
- В режиме WORK фон стал синим `#0f0f23`

### Как исправить:
```jsx
// В режиме WORK:
<div className="w-full h-full relative bg-black text-white">
    {/* НЕ bg-[#0f0f23], а bg-black */}
</div>
```

---

## ОШИБКА 3: Голосовой и текстовый ввод

### Было (правильно):
- **Текстовое поле** для ввода задачи
- **Кнопка микрофона** рядом
- Оба элемента видны и доступны

### Стало (неправильно):
- Только иконка микрофона
- Нет текстового поля
- Невозможно ввести задачу

### Как исправить:
```jsx
{/* Нижняя панель ввода */}
<div className="fixed bottom-0 left-0 right-0 p-4 bg-black/80 z-20">
    <div className="flex items-center gap-4 max-w-4xl mx-auto">
        {/* Текстовое поле */}
        <input
            type="text"
            placeholder="Введите задачу..."
            className="flex-1 bg-white/10 border border-white/20 rounded-lg px-4 py-3 text-white"
        />
        {/* Кнопка микрофона */}
        <button className="p-3 rounded-full bg-white/10 hover:bg-white/20">
            🎤
        </button>
        {/* Кнопка отправки */}
        <button className="p-3 rounded-full bg-cyan-500 hover:bg-cyan-400">
            →
        </button>
    </div>
</div>
```

---

## ОШИБКА 4: Облако точек в режиме WORK

### Было (правильно):
- Однородное облако точек (как на первом слайде)
- Чистое, без кластеров вокруг нод

### Стало (неправильно):
- Облако с кластерами (как на втором слайде)
- Точки сгруппированы вокруг 3 центров

### Как исправить:
- В режиме WORK используй `uProgress1 = 0`, `uProgress2 = 0`
- Или `uProgress2 = 1` только после создания задач

---

## ОБЩИЕ ПРАВИЛА

1. **НЕ МЕНЯЙ** то, что работает
2. **НЕ ДОБАВЛЯЙ** новые цвета фона
3. **СОХРАНЯЙ** существующий функционал
4. **ТЕСТИРУЙ** перед деплоем

---

## ДЕЙСТВИЯ

1. Верни Viewport3D_V3.tsx к предыдущей рабочей версии
2. Или исправь перечисленные ошибки
3. Убедись что:
   - Названия статичны
   - Фон чёрный везде
   - Есть текстовое поле для ввода
   - Облако однородное в WORK режиме

---

*Исправь это немедленно!*
