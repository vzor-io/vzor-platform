# VZOR Platform - Session Notes
**Last Updated:** 2026-02-06
**Server:** 95.174.95.209 (Cloud.ru)
**Current Branch:** development (main = эталон)

---

## ✅ ЧТО СДЕЛАНО СЕГОДНЯ

### 1. Панель задач (task-interface)
- **Проблема:** Панель появлялась автоматически, кнопка не работала
- **Решение:** Убрали 3 места автопоказа в коде
- **Результат:** ✅ Панель скрыта по умолчанию, появляется по клику на кнопку в левой панели
- **Крестик:** Закрывает панель (раньше очищал input)

### 2. Кнопки и UI
- **Левая панель (icon-bar):** Круглые кнопки с белым свечением, scale 1.15
- **Кнопка "Задачи":** Добавлена в левую панель, вызывает task-interface
- **Квадратная кнопка меню:** 34px, белое свечение (убрали бирюзовый)
- **Кнопка справа вверху:** Такая же как слева (34px, белое свечение, scale)
- **Кнопка NODES:** Небольшой контур, появляется только в Development
- **Все кнопки:** Единообразный hover и scale 1.15

### 3. Git и GitHub
- **Создан тег:** stable-v1.0 (эталонная версия)
- **Ветки:**
  - `main` - эталон (не трогать)
  - `development` - рабочая (активна сейчас)
- **Запушено на GitHub:** vzor-io/vzor-platform
- **SSH ключ:** Настроен на сервере

---

## 📁 СТРУКТУРА ПРОЕКТА

```
/home/vzor/vzor/
├── docker-compose.yml          # Оркестрация сервисов
├── config/
│   ├── nginx/
│   │   ├── default.conf        # Nginx конфиг (SSL, proxy)
│   │   ├── ssl/                # Самоподписанные сертификаты
│   │   └── www/
│   │       └── index.html      # ГЛАВНЫЙ ФАЙЛ (323KB, ~7500 строк)
│   └── prometheus/
├── data/                       # Docker volumes
└── SESSION_NOTES.md           # Этот файл
```

**Главный файл:** `/home/vzor/vzor/config/nginx/www/index.html`
- Standalone HTML приложение
- Three.js 3D визуализация
- Управление задачами (task-interface)
- Node editor для workflows

---

## 🔧 КАК РАБОТАЕТ СИСТЕМА

### Левая панель (icon-bar)
1. Квадратная кнопка слева вверху → открывает панель
2. Круглые кнопки: Invest, Design, Build, Sales, Files, **Задачи**, Settings
3. Кнопка "Задачи" → показывает task-interface внизу посередине

### Панель задач (task-interface)
- **По умолчанию:** Скрыта (CSS: `bottom: -100px; opacity: 0`)
- **Открытие:** Класс `active` → `bottom: 25px; opacity: 1`
- **Закрытие:** Крестик или повторный клик на кнопку "Задачи"
- **Функции:** Текстовый и голосовой ввод задач

### Кнопка NODES
- **Видимость:** `opacity: 0` по умолчанию
- **Появляется:** Только в drill-down режиме Development
- **Функция:** Открывает node editor для workflows

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### Решенные
- ✅ task-interface автопоказ (было 3 места)
- ✅ Кнопка задач не работала (общий обработчик мешал)
- ✅ Бирюзовая подсветка кнопок (теперь белая)
- ✅ NODES видна на всех этапах (теперь только в Development)

### Текущие
- Нет известных проблем

---

## 🚀 КОМАНДЫ

### Подключение к серверу
```bash
ssh -i C:\Users\vzor\Desktop\.ssh\id_ed25519 vzor@95.174.95.209
```

### Docker
```bash
cd /home/vzor/vzor
docker-compose ps                    # Статус сервисов
docker restart vzor-nginx           # Перезапуск nginx
docker-compose logs -f vzor-nginx   # Логи
```

### Git
```bash
cd /home/vzor/vzor
git status                          # Текущее состояние
git checkout development            # Рабочая ветка
git checkout main                   # Эталон
git log --oneline -10               # История
```

### Редактирование главного файла
```bash
# Создать бэкап перед изменениями
cp config/nginx/www/index.html config/nginx/www/index.html.backup

# После изменений
docker restart vzor-nginx
```

---

## 📝 АРХИТЕКТУРА КОДА

### task-interface - Ключевые части

**CSS (строка ~364):**
```css
#task-interface {
    bottom: -100px;    /* Скрыта */
    opacity: 0;
}
#task-interface.active {
    bottom: 25px;      /* Видна */
    opacity: 1;
}
```

**JavaScript обработчик (строка ~7051):**
```javascript
const tasksIconBtn = document.getElementById('tasks-icon-btn');
tasksIconBtn.addEventListener('click', (e) => {
    taskInterface.classList.toggle('active');
    // Закрыть icon-bar
});
```

**Общий обработчик icon-btn (строка ~7036):**
```javascript
document.querySelectorAll('#icon-bar .icon-btn').forEach(btn => {
    // Пропускает tasks-icon-btn
    if (btn.id === 'tasks-icon-btn') return;
    // ...
});
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. [ ] Интеграция Agent Zero API (уже есть /api/task endpoint)
2. [ ] Тестирование голосового ввода в task-interface
3. [ ] Добавление новых функций

---

## 💡 ВАЖНЫЕ ЗАМЕТКИ

- **Не трогать ветку main** - это эталон
- **Работать только на development**
- **Перед большими изменениями:** создать бэкап или коммит
- **После изменений index.html:** обязательно `docker restart vzor-nginx`
- **Очистка кэша:** Ctrl+F5 в браузере после изменений

---

## 🔗 ССЫЛКИ

- **Сайт:** https://95.174.95.209
- **GitHub:** https://github.com/vzor-io/vzor-platform
- **Эталон:** ветка main, тег stable-v1.0
- **Рабочая:** ветка development

---

## 📞 КАК ПРОДОЛЖИТЬ ЗАВТРА

Напиши мне:

**"Прочитай SESSION_NOTES.md и продолжим работу"**

Я прочитаю этот файл и буду знать весь контекст.
